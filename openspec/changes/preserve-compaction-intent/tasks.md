## 1. Confirm the Simplified Implementation Baseline

- [x] 1.1 Before changing code, verify the implementation workspace uses `7087ac8cf9b27a780d08d5232f2fd501f94258ec` or a reviewed descendant with the same six-field/manual-lifecycle contract; record `git rev-parse HEAD` and `git status --short`. If the workspace is still the dirty seven-field `19aa801`, stop and report the mismatch without pull/reset/overwrite or branch operations. Completion evidence: main verified at `7087ac8cf9b27a780d08d5232f2fd501f94258ec` after the user explicitly authorized discarding tracked local edits and resetting main; the four planning artifacts plus `.openspec.yaml` retained identical SHA-256 hashes. Untracked planning/tool files were preserved; no `git clean` was run.

## 2. Build and Verify Original User Evidence

- [x] 2.1 Add `internal/continuity-evidence.ts` source-catalogue helpers using raw current-branch user entries, original content-block indices, host kept boundaries, and the ordered/bounded windows in design Decision 2. Add unit cases in `tests/continuity.test.ts` for bootstrap, split-turn user prefix, kept recent correction, long-message head/tail gaps, absent boundaries, and exclusion of assistant/tool/generated-summary text; verify with `npx vitest --run tests/continuity.test.ts`.
- [x] 2.2 Implement exact source-window matching, canonical copying, absolute UTF-16 offsets, built-in SHA-256 identities, and duplicate handling. Verify tests accept exact Chinese/multiline/escaped text and reject changed wording, unknown source IDs, cross-gap selections, cross-branch sources, model-supplied paths, invalid Unicode spans, and overlong selections without trimming them. Completion evidence: the focused source-to-context test first failed because no compaction result was produced, then passed after implementation. The final full regression has 109 passing tests (97 continuity, ten corpus-structure and two cache tests), including acceptance/rejection cases and canonical identity checks. Stock Pi projection and both packed-host scenarios verify exact quoted text in continuation requests. These are mechanical source/context checks, not real-model behavior.
- [x] 2.3 Implement version-1 `details.continuity` validation and rehydration from the latest current-branch compaction only, including content-identity checks and `initial/carried/rebuilt` coverage basis. Verify tests cover valid restart rehydration, altered/unavailable source text, unknown versions, no metadata, native fallback between continuity results, and no borrowing of stale evidence across branches or a newer unknown compaction.
- [x] 2.4 Implement carry-forward, validated explicit retirement with a newer user-source quotation, and deterministic whole-quote budget allocation. Verify tests retain unmentioned prior evidence, apply an explicit `8080` to `8081` correction, reject unsupported/assistant-only retirements, prioritize constraints/corrections, and expose budget omissions without shortening quotations.

## 3. Integrate Extraction and Budgets

- [x] 3.1 In `internal/continuity-core.ts`, retain the six-field `parseSummary` boundary and add the strict `summary/quotes/retire` response envelope. Replace per-category 24-item rejection with the 128-item aggregate ceiling; remove independent 16000/2000/1000-character semantic gates and use the shared `16 * R` raw-response guard, shape/types, aggregate items, and rendered budget from design Decision 5. Keep the bounded evidence arrays. Verify 25 short Open items and complete longer semantic strings within the actual bounds survive; excessive aggregate/raw results fail; individual invalid evidence can coexist with a valid semantic summary.
- [x] 3.2 Update `SUMMARY_SYSTEM_PROMPT` and synthesis input construction with initial/update branches, the six-field mapping in design Decision 6, explicit soft targets/safety bounds, and retirement instructions. Separate prior summary, new history, split-turn prefix, source catalogue, and final host `customInstructions`; filter only the existing `CONTINUE_TYPE` custom messages before serialization. Verify captured requests preserve a task-defining prefix and similarly worded genuine user text, distinguish focus/data and tool attempts/results, retain the latest unanswered question or pending decision, apply corrections/reversals, and allow no active request when all work is resolved. Do not add `/continuity` arguments or intercept ordinary `/compact`.
- [x] 3.3 Implement the content-scaled `hermesTarget`, adapted `responseTarget`, host-derived `R/G`, and shared raw limit from design Decision 5, with public `estimateTokens`, bounded catalogue reduction, and 4096-token request headroom. Omit the plugin's explicit request `maxTokens` while preserving auth/signal/cache/routing options. Verify target scaling and small-capacity cases, and accept complete above-target results within safety/render bounds; input exhaustion triggers fallback without a plugin request. Verify empty/length-limited output is neither committed nor repaired/retried by the plugin. Distinguish captured plugin options from SDK/provider-native output limits rather than asserting unlimited generation.
- [x] 3.4 Integrate evidence reconstruction/merge into continuity-owned `session_before_compact` events without new session-start/tree/compaction state listeners. Verify a sequence of three compactions keeps original quotation identities/text when later model responses omit them, including fresh-extension-instance restart and legacy bootstrap.

## 4. Render Visible Continuity and Degradation

- [x] 4.1 Extend rendering with the six semantic sections, source-labelled JSON-escaped original quotations, selected-coverage wording, omission/rejection/retirement counts, and bounded transcript recovery references. Replace the fixed 6144 total with host-derived `R` (normally 16384); allocate evidence up to 1536 estimated tokens and actual remaining `R`, including escaped text, labels, locators, and coverage scaffolding. Verify complete above-target semantic output is not rejected solely for missing the soft target, whole-quote omissions are disclosed, semantic lists are never shortened to fit, decoded quotations equal originals, and zero mechanical omissions never claim exhaustive coverage.
- [x] 4.2 Extend `fileDetails` to merge available latest-compaction file details with `preparation.fileOps`, preserving complete compatible lists while rendering a modified-first 512-estimated-token file section. Verify repeated compaction with no new file operations retains prior modified paths, duplicates become modified only, file display shrinks before evidence under the total bound, and persisted file lists remain intact.
- [x] 4.3 Add bounded synthesis outcome reasons and UI/headless diagnostics for missing model/auth, provider failure, empty/incomplete output, invalid schema/JSON, invalid boundaries, and budget failure; keep explicit user/host cancellation distinct. Preserve the user's all-native-fallback policy for every extraction failure rather than importing Hermes terminal-failure cancellation. Verify no diagnostic exposes raw conversation/provider/credential text, creates a chat message, retries plugin extraction, or causes an extra continuation; valid semantic output with bad quotes reports coverage rather than falling back. Native fallback may make additional host requests and is not guaranteed success or source-verified continuity.

## 5. Verify Host Lifecycle and Semantic Acceptance Separately

- [x] 5.1 Extend the existing faux-provider scenarios in `scripts/lib/run-pi-continuity-host-e2e.mjs` for the new envelope and source-backed details, retaining commit-before-continue, duplicate, cancellation, native manual fallback, ordinary compact, automatic/overflow ownership, auth reuse, and usage assertions. Cover empty/truncated/provider failures permitting native fallback, successful native commit permitting only the existing one-time manual continuation, and failed native fallback producing no continuation. Capture outgoing request/output-limit behavior at the harness boundary, identify that boundary explicitly, and count plugin versus native requests separately; an options-object check does not prove every provider's wire behavior. Verify with `npm run test:e2e:upstream-pi` and `npm run test:e2e:xz-dev-pi`; record exact host versions and results independently.
- [x] 5.2 Verify actual host context projection and persisted-session reuse with a three-compaction/restart scenario through the existing harness: selected original constraint and cumulative file section are visible to the continuing model, source metadata survives JSONL reload, a branch cannot inherit sibling evidence, and native fallback produces a visibly rebuilt next selection. Completion evidence: assertions over persisted entries and captured continuation requests, not only mocked renderer snapshots.
- [x] 5.3 Add a small synthetic evaluation corpus using the existing extraction/provider seam, without another framework or service. Follow design Decision 10: positive/negative constraints, `8080`→`8081`, completed code with failed tests or pending approval, unanswered questions/discussions, stop/reversal signals, a tool attempt without a success result, a failure later repaired with exact command/error, and a fully resolved conversation. Define expected state/spans and actual next-action outcomes after at least three compactions and reload. Verify fixture structure deterministically. Only with approved model access/data/cost, compare the selected baseline, this change, and pinned Hermes runtime on comparable inputs/downstream model; record reference/model revisions, retained-context policy, targets, actual stops/limits, plugin/native requests, semantic/action failures, and usage/cost. Wrong continuation behavior fails acceptance even when schema/quotation checks pass. If real-model runs are unavailable or not approved, leave behavioral fidelity and Hermes comparison explicitly unverified rather than infer them from fixtures.
- [x] 5.4 Run `npm run check` and `npm test` with existing dependencies, and reconcile failures against pre-existing workspace state. Completion evidence: command outputs, changed-file list, and explicit separation between deterministic source/lifecycle passes and the semantic evaluation status from 5.3; no commit, push, release, or deployment is implied.

### Final verification evidence (implementation/mechanical closeout; behavior unverified)

- **Final local checks, 2026-09-08, Node v26.8.1:** `npm test` passed **109/109** across three files (97 continuity, ten corpus-structure, two cache); `npm run check`, `openspec validate preserve-compaction-intent --strict`, and `git diff --check` passed. These are local command results, not a claim that GitHub CI was run.
- The added retirement/reselection test first failed at the intended assertion (`expected true to be false`): quote merging re-added a just-retired identity. A response-local retired-ID set now rejects that contradictory echo, without persistent tombstones. The complete continuity file then passed **97/97**; the final full regression and both host reruns include this fix.
- 2.1–2.3 and 3.1–4.3 evidence: raw source/block/span validation; branch, bootstrap and split-turn boundaries; real JSONL reload and integrity rejection; three compactions with omitted selections; host-derived input/raw/render budgets; strict six fields and aggregate 128-item ceiling; request partitioning/auth/signal/options; bounded failure diagnostics; file carry-forward and modified-first display reduction. Supported retirement, omission carry-forward, budget-priority and same-response conflict cases now pass.
- **5.1–5.2 final host reruns after the runtime fix and README changes:** `npm run test:e2e:upstream-pi` passed at `b2602be77cb7b0de45dd616407fd210daa48aa75`; `npm run test:e2e:xz-dev-pi` passed at `367c709ac2114fca17e6dc3daa3641dbbd50a27e`. Both report Pi **0.85.1**, with **37 faux requests per host**. The harness verifies on-disk commit before continuation, three rounds with fresh extension/JSONL reload, exact quotes and files in continuation input, correction, sibling isolation, native gap, fallback success/failure, cancellation, duplicate commands, and Pi-owned threshold/overflow. Auth reuse and persisted normalized usage are checked.
- Request-limit observation boundary: **faux response factory after SDK normalization, not HTTP wire**. Plugin requests had no `maxTokens` value; native requests had `13107`. Each invalid/empty/truncated/provider-error scenario made one plugin request, one native request and one continuation; failed native fallback made no continuation. No claim about every provider's wire limits or unlimited generation follows.
- Host build repair: current hosts require new workspace dependencies (`pi-telemetry`, `chord`, protocol/client/server and session backend). The harness now invokes the host's existing root `build:offline` chain; cache recipe **4**, fixture outputs and CI cache keys agree. Faux recomputes usage, so host checks validate its normalized persisted usage; the controlled unit seam checks exact usage forwarding. Threshold is exercised after the pre-prompt check via a scoped model-window change, not fabricated usage or a live-model capacity benchmark.
- Earlier workspace/signing issues were resolved: the obsolete untracked integration test was removed under the discard authorization; only temporary fixture commits use process-scoped `-c commit.gpgsign=false`. No persistent signing/auth configuration was changed.
- **5.3:** `tests/fixtures/continuity-corpus.json` contains all nine agreed topic groups with expected six-field state, exact user spans, obsolete requests, and next-action/forbidden-action oracles. `tests/continuity-corpus.test.ts` passes ten deterministic structure/annotation checks. `tests/fixtures/README.md` defines the three-compaction/reload, frozen baseline/candidate/Hermes comparison and required receipts through the existing extraction/provider seam, with no new framework/service. The 5.3 checkbox covers the implemented corpus/protocol and its explicitly specified **unapproved-run branch**; it is **not a semantic acceptance pass**. The nine cases have not been behaviorally replayed with real models, and no real-model or Hermes comparison was run.
- **6.1:** `README.md` now documents the two-feature lifecycle, exact six fields versus reference metadata, bounded selection and recovery, priority/fallback/cancellation behavior, budgets and provider-limit observation boundaries, persistence/files, reference order and provenance limits. Command examples, field names, limits and host receipt claims were checked against the implementation and passing local tests.
- Changed implementation/test/CI/docs paths: `internal/continuity-core.ts`, new `internal/continuity-evidence.ts`, `tests/continuity.test.ts`, `tests/host-e2e-cache.test.ts`, new `tests/continuity-corpus.test.ts` and `tests/fixtures/{continuity-corpus.json,README.md}`, `scripts/lib/run-pi-continuity-host-e2e.mjs`, `.github/workflows/ci.yml`, and `README.md`. Progress is tracked only in this file. No project commit, push, release or deployment was performed by these verification runs.
- **Scope of completion:** all 17 implementation tasks are accounted for, including 5.3's explicit unverified-behavior branch. The tested pre-commit baseline was `7087ac8cf9b27a780d08d5232f2fd501f94258ec` on `main`, with this candidate present as uncommitted changes. `.pi/`, `.serena/`, `PLAN/` and `openspec/` remain preserved. Real-model fidelity and the baseline/candidate/Hermes behavioral comparison require separate model/data/cost approval. Exact copying, schema validity, successful persistence and 37 faux requests are not substitutes for that validation. The change is not archived.

## 6. Document the Bounded Contract

- [x] 6.1 Update `README.md` with the unchanged two-feature lifecycle, Hermes-primary/pi-continue-secondary reference order, selected-not-exhaustive quotation guarantee, six semantic fields versus versioned source-reference details, content-scaled soft target versus SDK/model limits and host-derived safety bounds, all-native-fallback choice and possible extra host calls/failure, restart/legacy/native-gap behavior, recovery references, and stock user-role provenance limits. Verify every example matches unit/host output and clearly separates measured behavior from unverified effects; no implication that no plugin cap means unlimited generation, or that JSON validity/successful compaction proves complete intent preservation.
